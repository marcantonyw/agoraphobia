---
const { headings } = Astro.props;
---
<ul style="list-style: none; padding: 0; margin: 0;">
  {headings.map(heading => (
    <li style="margin-bottom: 0.5rem;">
      <a href={`#${heading.id}`} style="color: inherit; text-decoration: none;">
        {heading.text}
      </a>
    </li>
  ))}
</ul>